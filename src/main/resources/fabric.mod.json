{
	"schemaVersion": 1,
	"name": "${mod_name}",
	"id": "${mod_id}",
	"version": "${mod_version}",
	"description": "${mod_description}",
	"authors": [
		"Roland A"
	],
	"icon": "assets/${mod_id}/icon.png",
	"license": "ARR",
	"environment": "*",
	"entrypoints": {
		"main": [
			{
				"adapter": "kotlin",
				"value": "roland_a.mc_mods.template.Mod"
			}
		]
	},
	"mixins": [
		"${mod_id}.mixins.json"
	],
	"accessWidener": "${mod_id}.accesswidener",
	"depends": {
		"minecraft": "${mc_version_requirement}",
		"fabric-language-kotlin": "${fabric_kotlin_version_requirement}"
	}
}
